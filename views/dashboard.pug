extends layout
	
block content
    .card(width='500px')
        .profilename #{user.username}
        ul
            each article, i in articles
                a(href="/article/"+article._id)= article.title
                img.dashImg(src=article.image, alt='no image')